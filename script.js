import { createTransactionsState, formatCurrency, getFilteredTransactions, validateTransaction, TRANSACTION_CATEGORIES } from "./transactions.js";
import { createCharts } from "./chart.js";
import {
  startFirebase, firebase, collection, doc, addDoc, setDoc, updateDoc, deleteDoc, query, where, onSnapshot,
  serverTimestamp, onAuthStateChanged, createUserWithEmailAndPassword, signInWithEmailAndPassword, GoogleAuthProvider,
  signInWithPopup, sendPasswordResetEmail, signOut, updateProfile,
} from "./firebase.js";

const $ = (id) => document.getElementById(id);
const body = document.body;
const state = createTransactionsState([]);
let user = null, settings = { theme: "dark", language: "en", notifications: true }, goals = [], bills = [], notifications = [], conversation = [], currentPage = 1, unsubscribers = [];
const categories = new Set(TRANSACTION_CATEGORIES);
const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (char) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;", "'":"&#039;" }[char]));
const validDate = (value) => /^\d{4}-\d{2}-\d{2}$/.test(value) && !Number.isNaN(new Date(`${value}T12:00:00`).getTime());
const cleanText = (value, max = 140) => String(value ?? "").trim().replace(/[<>]/g, "").slice(0, max);
const own = (data = {}) => ({ ...data, uid: user.uid, updatedAt: serverTimestamp() });
const status = (message, error = false) => { const node = $("formError"); if (node) { node.textContent = message; node.className = error ? "form-error" : "form-success"; } };
const money = (value) => formatCurrency(Number(value) || 0);
const monthTotals = () => state.transactions.reduce((total, item) => {
  const now = new Date(), date = new Date(`${item.date}T12:00:00`);
  if (date.getFullYear() === now.getFullYear() && date.getMonth() === now.getMonth()) total[item.type === "income" ? "income" : "expenses"] += Number(item.amount) || 0;
  return total;
}, { income: 0, expenses: 0 });
const currentExpenses = () => monthTotals().expenses;

function applyTheme() {
  body.dataset.theme = settings.theme === "light" ? "light" : "dark";
  $("themeToggle").textContent = body.dataset.theme === "dark" ? "☾" : "☀";
  if ($("themeSelect")) $("themeSelect").value = body.dataset.theme;
}
async function saveSettings(patch) {
  settings = { ...settings, ...patch };
  applyTheme();
  if (!user) return;
  await setDoc(doc(firebase().db, "settings", user.uid), own(settings), { merge: true });
}
function renderAll() { renderTransactions(); renderBudget(); renderAnalytics(); renderGoals(); renderBills(); renderNotifications(); renderSummary(); createCharts($("lineChart"), $("categoryChart"), state.transactions, goals, bills); }
function renderAnalytics() {
  const totals = monthTotals(), net = totals.income - totals.expenses;
  const balance = state.transactions.reduce((sum, item) => sum + (item.type === "income" ? 1 : -1) * Number(item.amount || 0), 0);
  $("analyticsExpenseValue").textContent = money(totals.expenses); $("analyticsIncomeValue").textContent = money(totals.income); $("analyticsSavingsValue").textContent = money(net); const projection = Math.max(0, net); if ($("projectedSavingsValue")) $("projectedSavingsValue").textContent = money(projection);
  $("balanceValue").textContent = money(balance); $("monthlyIncomeValue").textContent = money(totals.income); $("monthlyExpenseValue").textContent = money(totals.expenses); $("monthlySavingsValue").textContent = money(net);
}
function renderTransactions() {
  const filters = state.filters, search = $("searchInput")?.value || "";
  filters.search = search; filters.type = $("typeFilter")?.value || "all"; filters.category = $("categoryFilter")?.value || "all";
  const sort = $("sortSelect")?.value || "date-desc", size = Number($("pageSizeSelect")?.value || 10);
  const entries = getFilteredTransactions(state.transactions, filters).sort((a,b) => sort === "amount-desc" ? b.amount-a.amount : sort === "amount-asc" ? a.amount-b.amount : sort === "date-asc" ? a.date.localeCompare(b.date) : b.date.localeCompare(a.date));
  const pages = Math.max(1, Math.ceil(entries.length / size)); currentPage = Math.min(currentPage, pages);
  const list = $("transactionsList");
  list.innerHTML = entries.length ? entries.slice((currentPage-1)*size, currentPage*size).map((item) => `<article class="transaction-row"><div class="transaction-main"><span class="type-badge ${item.type}">${esc(item.type)}</span><div class="transaction-meta"><strong>${esc(item.description)}</strong><p>${esc(item.category)} · ${esc(item.date)}${item.recurring ? " · Recurring" : ""}</p></div></div><div class="transaction-actions"><span class="amount ${item.type === "income" ? "positive" : "negative"}">${item.type === "income" ? "+" : "-"}${money(item.amount)}</span><button class="action-btn" data-action="edit" data-id="${esc(item.id)}">Edit</button><button class="action-btn delete" data-action="delete" data-id="${esc(item.id)}">Delete</button></div></article>`).join("") : '<div class="empty-state">No transactions yet. Add your first income or expense.</div>';
  $("pageInfoText").textContent = `Page ${currentPage} of ${pages}`;
}
function budget() { return Number(settings.monthlyBudget) || 0; }
function renderBudget() {
  const planned = budget(), spent = currentExpenses(), remaining = planned - spent, percent = planned ? Math.min(100, Math.round((spent/planned)*100)) : 0;
  $("budgetInput").value = planned || ""; $("monthlyBudgetValue").textContent = money(planned); $("spentBudgetValue").textContent = money(spent); $("remainingBudgetValue").textContent = money(remaining); $("budgetProgressBar").style.width = `${percent}%`; $("budgetPercentText").textContent = `${percent}%`;
  $("budgetStatusText").textContent = !planned ? "Set a budget" : remaining < 0 ? "Over budget" : percent >= 80 ? "Near limit" : "On track";
  $("budgetWarning").textContent = !planned ? "Set a monthly budget to track progress." : remaining < 0 ? `You exceeded your budget by ${money(Math.abs(remaining))}.` : "You are within your monthly budget.";
}
function renderGoals() { $("goalsList").innerHTML = goals.length ? goals.map((goal) => { const pct = Math.min(100, Math.round((Number(goal.current)/Number(goal.target))*100)); return `<article class="stack-item"><div class="stack-header"><strong>${esc(goal.name)}</strong><span>${pct}%</span></div><div class="goal-progress-track compact"><div class="goal-progress-bar" style="width:${pct}%"></div></div><p>${money(goal.current)} / ${money(goal.target)} · Due ${esc(goal.deadline)}</p></article>`; }).join("") : '<div class="empty-state">No savings goals yet.</div>'; }
function renderBills() { $("billsList").innerHTML = bills.length ? [...bills].sort((a,b)=>a.date.localeCompare(b.date)).map((bill)=>`<article class="stack-item"><div class="stack-header"><strong>${esc(bill.name)}</strong><span>${money(bill.amount)}</span></div><p>Due ${esc(bill.date)}</p></article>`).join("") : '<div class="empty-state">No bills scheduled.</div>'; }
function renderNotifications() { $("notificationsList").innerHTML = notifications.length ? notifications.slice(0,5).map((n)=>`<li class="notification-item">${esc(n.message)}</li>`).join("") : '<li class="empty-state">No new notifications.</li>'; }
function renderSummary() {
  const totals = monthTotals(), net = totals.income - totals.expenses, next = [...bills].filter((bill)=>bill.date >= new Date().toISOString().slice(0,10)).sort((a,b)=>a.date.localeCompare(b.date))[0], goal = goals[0], progress = goal ? Math.min(100,Math.round(goal.current/goal.target*100)) : 0;
  $("summaryNetValue").textContent = money(net); $("summaryTrendValue").textContent = net >= 0 ? "Positive flow" : "Watch spending"; $("summaryNextBillValue").textContent = next ? money(next.amount) : "—"; $("summaryNextBillDate").textContent = next ? `Due ${next.date}` : "No upcoming bills"; $("summaryGoalValue").textContent = `${progress}%`; $("summaryGoalLabel").textContent = goal ? esc(goal.name) : "No active goal"; $("goalProgressBar").style.width = `${progress}%`; $("goalProgressText").textContent = `${progress}%`; $("savingsGoalTitle").textContent = goal?.name || "No goal selected"; $("goalCurrentValue").textContent = money(goal?.current); $("goalTargetValue").textContent = money(goal?.target);
  $("recentActivityList").innerHTML = state.transactions.slice().sort((a,b)=>b.date.localeCompare(a.date)).slice(0,6).map((item)=>`<li class="activity-item"><span>${esc(item.description)}</span><strong>${money(item.amount)}</strong></li>`).join("") || '<li class="empty-state">No activity yet.</li>';
}
function subscribe(collectionName, apply) { unsubscribers.push(onSnapshot(query(collection(firebase().db, collectionName), where("uid", "==", user.uid)), (snapshot) => { apply(snapshot.docs.map((item)=>({ id:item.id, ...item.data() }))); renderAll(); }, (error)=>status(`Could not load ${collectionName}: ${error.message}`, true))); }
function subscribeData() {
  unsubscribers.forEach((stop)=>stop()); unsubscribers=[];
  subscribe("transactions", (items)=>state.transactions=items); subscribe("goals", (items)=>goals=items); subscribe("bills", (items)=>bills=items); subscribe("notifications", (items)=>notifications=items.sort((a,b)=>String(b.createdAt?.seconds||0).localeCompare(String(a.createdAt?.seconds||0))));
  unsubscribers.push(onSnapshot(doc(firebase().db,"settings",user.uid), (snapshot)=> { settings={...settings,...(snapshot.data()||{})}; applyTheme(); renderAll(); }));
}
async function note(message) { if (settings.notifications) await addDoc(collection(firebase().db,"notifications"), own({ message: cleanText(message, 180), createdAt: serverTimestamp(), read: false })); }
async function saveTransaction(event) {
  event.preventDefault();
  const payload={ type: $("typeInput").value, date:$("dateInput").value, category:cleanText($("categoryInput").value,40), description:cleanText($("descriptionInput").value), amount:Number($("amountInput").value), recurring:$("recurringInput").checked, frequency:$("frequencyInput").value, receipt:"" };
  const id=$("transactionId").value;
  const error=validateTransaction(payload,state.transactions,id); if(error) return status(error,true);
  try { if(!user?.uid) throw new Error("No authenticated user."); const reference=collection(firebase().db,"transactions");
    if(id) { await updateDoc(doc(firebase().db,"transactions",id),own(payload)); } else { await addDoc(reference,own({ ...payload, createdAt:serverTimestamp() })); await note(`${payload.type === "income" ? "Income recorded" : "Expense recorded"}: ${payload.description}`); }
    closeModal();
  } catch(error) { console.error("[TX] FAIL write",error); status(error.message,true); }
}
function openModal(id="") { $("transactionForm").reset(); $("transactionId").value=id; $("dateInput").value=new Date().toISOString().slice(0,10); if(id) { const item=state.transactions.find((entry)=>entry.id===id); if(item) Object.entries(item).forEach(([key,value])=> { const input=$(`${key}Input`); if(input && typeof value !== "object") input.value=value; }); $("recurringInput").checked=Boolean(item.recurring); } $("transactionModal").classList.remove("hidden"); $("transactionModal").setAttribute("aria-hidden","false"); $("descriptionInput").focus(); }
function closeModal() { $("transactionModal").classList.add("hidden"); $("transactionModal").setAttribute("aria-hidden","true"); status(""); }
async function askAI(prompt) { const response=await fetch("/api/assistant",{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${await user.getIdToken()}`},body:JSON.stringify({prompt,transactions:state.transactions,goals,bills,budget:budget()})}); const data=await response.json(); if(!response.ok) throw new Error(data.error||"Assistant is unavailable."); return data.text; }
async function handleAssistant(eventOrText) { if(eventOrText?.preventDefault) eventOrText.preventDefault(); const prompt=typeof eventOrText === "string" ? eventOrText : $("assistantInput").value; if(!cleanText(prompt,500)) return; $("assistantInput").value=""; conversation.push({role:"user",text:prompt}); renderConversation(); try { const text=await askAI(prompt); conversation.push({role:"assistant",text}); renderConversation(); } catch(error) { conversation.push({role:"assistant",text:error.message}); renderConversation(); } }
function download(name, content, type) { const url=URL.createObjectURL(new Blob([content],{type})); const link=document.createElement("a"); link.href=url; link.download=name; link.click(); URL.revokeObjectURL(url); }
function renderConversation() { const chat=$("assistantChat"); chat.replaceChildren(...conversation.map((entry)=> { const row=document.createElement("div"); row.className=`assistant-message ${entry.role}`; const bubble=document.createElement("div"); bubble.className="message-bubble"; const title=document.createElement("strong"); title.textContent=entry.role === "user" ? "You" : "PocketWise AI"; const p=document.createElement("p"); p.textContent=entry.text; bubble.append(title,p); row.append(bubble); return row; })); }
async function authSubmit(event) { event.preventDefault(); const email=$("authEmail").value.trim(), password=$("authPassword").value, name=cleanText($("authName").value,80); if(!/^\S+@\S+\.\S+$/.test(email)) return $("authSubtitle").textContent="Enter a valid email address."; if(password.length<8) return $("authSubtitle").textContent="Use at least 8 characters for your password."; try { let credential; if($("authName").hidden) credential=await signInWithEmailAndPassword(firebase().auth,email,password); else { if(name.length<2) throw new Error("Enter your full name."); credential=await createUserWithEmailAndPassword(firebase().auth,email,password); await updateProfile(credential.user,{displayName:name}); await setDoc(doc(firebase().db,"users",credential.user.uid),{uid:credential.user.uid,email:credential.user.email,displayName:name,createdAt:serverTimestamp()},{merge:true}); } } catch(error) { $("authSubtitle").textContent=error.message.replace("Firebase: ",""); } }
function bind() {
  $("transactionForm").addEventListener("submit",saveTransaction); $("openModalBtn").addEventListener("click",()=>openModal()); $("addExpenseBtn").addEventListener("click",()=>openModal()); $("closeModalBtn").addEventListener("click",closeModal); $("cancelModalBtn").addEventListener("click",closeModal);
  $("transactionsList").addEventListener("click",async(event)=> { const button=event.target.closest("button[data-action]"); if(!button)return; if(button.dataset.action==="edit")openModal(button.dataset.id); if(button.dataset.action==="delete" && confirm("Delete this transaction?")) await deleteDoc(doc(firebase().db,"transactions",button.dataset.id)); });
  ["searchInput","typeFilter","categoryFilter","sortSelect","pageSizeSelect"].forEach((id)=>$(id)?.addEventListener("input",()=>{currentPage=1;renderTransactions();})); $("prevPageBtn").addEventListener("click",()=>{currentPage=Math.max(1,currentPage-1);renderTransactions();}); $("nextPageBtn").addEventListener("click",()=>{currentPage++;renderTransactions();});
  $("saveBudgetBtn").addEventListener("click",async()=>{const value=Number($("budgetInput").value); if(!Number.isFinite(value)||value<=0)return status("Enter a valid monthly budget.",true); await saveSettings({monthlyBudget:value}); await note(`Monthly budget set to ${money(value)}.`);}); $("themeToggle").addEventListener("click",()=>saveSettings({theme:body.dataset.theme === "dark" ? "light":"dark"})); $("themeSelect")?.addEventListener("change",(e)=>saveSettings({theme:e.target.value})); $("notificationsToggle")?.addEventListener("change",(e)=>saveSettings({notifications:e.target.checked}));
  $("goalForm").addEventListener("submit",async(e)=>{e.preventDefault();const goal={name:cleanText($("goalNameInput").value,80),target:Number($("goalTargetInput").value),current:Number($("goalCurrentInput").value),deadline:$("goalDeadlineInput").value};if(goal.name.length<2||!Number.isFinite(goal.target)||goal.target<=0||goal.current<0||goal.current>goal.target||!validDate(goal.deadline))return status("Enter a valid goal, amount, progress, and deadline.",true);await addDoc(collection(firebase().db,"goals"),own({...goal,createdAt:serverTimestamp()}));e.target.reset();});
  $("billForm").addEventListener("submit",async(e)=>{e.preventDefault();const bill={name:cleanText($("billNameInput").value,80),amount:Number($("billAmountInput").value),date:$("billDateInput").value};if(bill.name.length<2||!Number.isFinite(bill.amount)||bill.amount<=0||!validDate(bill.date))return status("Enter a valid bill name, amount, and due date.",true);await addDoc(collection(firebase().db,"bills"),own({...bill,createdAt:serverTimestamp()}));await note(`${bill.name} is due ${bill.date}.`);e.target.reset();});
  $("exportBtn")?.addEventListener("click",()=>download("pocketwise-transactions.json",JSON.stringify(state.transactions,null,2),"application/json")); $("exportCsvBtn")?.addEventListener("click",()=>{const keys=["date","type","category","description","amount","recurring","frequency"];download("pocketwise-transactions.csv",[keys.join(","),...state.transactions.map((item)=>keys.map((key)=>`"${String(item[key]??"").replaceAll("\\\"","\\\"\\\"")}"`).join(","))].join("\n"),"text/csv;charset=utf-8");}); $("importBtn")?.addEventListener("click",()=>$("importInput").click()); $("importInput")?.addEventListener("change",async(event)=>{try { const records=JSON.parse(await event.target.files[0].text()); if(!Array.isArray(records)) throw new Error("Import must be a JSON array."); for(const record of records) { const payload={type:record.type,date:record.date,category:cleanText(record.category,40),description:cleanText(record.description),amount:Number(record.amount),recurring:Boolean(record.recurring),frequency:record.frequency||"monthly",receipt:""}; const error=validateTransaction(payload,state.transactions); if(error) throw new Error(error); await addDoc(collection(firebase().db,"transactions"),own({...payload,createdAt:serverTimestamp()})); } status("Transactions imported successfully."); } catch(error) { status(error.message||"Unable to import transactions.",true); } finally { event.target.value=""; }}); $("clearDataBtn")?.addEventListener("click",async()=>{if(!confirm("Delete all of your PocketWise financial data? This cannot be undone."))return; for(const name of ["transactions","goals","bills","notifications"]) for(const item of await new Promise((resolve,reject)=>{const stop=onSnapshot(query(collection(firebase().db,name),where("uid","==",user.uid)),(snapshot)=>{stop();resolve(snapshot.docs);},reject)})) await deleteDoc(item.ref); await deleteDoc(doc(firebase().db,"settings",user.uid));}); $("assistantForm").addEventListener("submit",handleAssistant); document.querySelectorAll(".suggestion-chip").forEach((button)=>button.addEventListener("click",()=>handleAssistant(button.dataset.text))); $("authForm").addEventListener("submit",authSubmit); $("authSwitchBtn").addEventListener("click",()=>{const register=$("authName").hidden;$("authName").hidden=!register;$("authName").required=register;$("authTitle").textContent=register?"Create Account":"Sign In";$("authSwitchBtn").textContent=register?"Already have an account?":"Need an account?";}); $("googleSignInBtn")?.addEventListener("click",async()=>{try { await signInWithPopup(firebase().auth,new GoogleAuthProvider()); } catch(error) { $("authSubtitle").textContent=error.message.replace("Firebase: ",""); }}); $("forgotPasswordBtn")?.addEventListener("click",async()=>{const email=$("authEmail").value.trim();if(!/^\S+@\S+\.\S+$/.test(email))return $("authSubtitle").textContent="Enter your email first.";try { await sendPasswordResetEmail(firebase().auth,email); $("authSubtitle").textContent="Password reset email sent."; } catch(error) { $("authSubtitle").textContent=error.message.replace("Firebase: ",""); }}); $("logoutBtn")?.addEventListener("click",async()=>{try { await signOut(firebase().auth); } catch(error) { $("authSubtitle").textContent=error.message.replace("Firebase: ",""); }}); const scrollToSection=(id)=>$(id)?.scrollIntoView({behavior:"smooth"}); $("viewAnalyticsBtn")?.addEventListener("click",()=>scrollToSection("analyticsSection")); $("openAssistantBtn")?.addEventListener("click",()=>scrollToSection("assistantSection")); $("notificationsBtn")?.addEventListener("click",()=>scrollToSection("notificationsList")); $("addGoalBtn")?.addEventListener("click",()=>$("goalNameInput")?.focus()); $("addBillBtn")?.addEventListener("click",()=>$("billNameInput")?.focus()); document.querySelectorAll(".nav-links a[data-section]").forEach((link)=>link.addEventListener("click",(event)=>{event.preventDefault(); document.querySelectorAll(".nav-links a").forEach((item)=>item.classList.toggle("active",item===link)); scrollToSection(`${link.dataset.section}Section`);}));
}
async function boot() { try { await startFirebase(); bind(); onAuthStateChanged(firebase().auth, async(account)=> { user=account; $("authView").classList.toggle("hidden",!!account); $("appView").classList.toggle("hidden",!account); if(account) { const displayName = account.displayName || account.email; $("profileName") && ($("profileName").textContent=displayName); const avatar=$(".avatar"); if(avatar) avatar.textContent=displayName.split(/\s+/).map((part)=>part[0]).join("").slice(0,2).toUpperCase(); await setDoc(doc(firebase().db,"users",account.uid),{uid:account.uid,email:account.email,displayName,updatedAt:serverTimestamp()},{merge:true}); subscribeData(); renderConversation(); } else { unsubscribers.forEach((stop)=>stop()); unsubscribers=[]; state.transactions=[]; goals=[]; bills=[]; notifications=[]; renderAll(); } }); } catch(error) { $("authSubtitle").textContent="Service setup is required: "+error.message; } }
if ("serviceWorker" in navigator) window.addEventListener("load",()=>navigator.serviceWorker.register("/sw.js").catch(()=>{}));
boot();
